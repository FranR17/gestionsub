# Registro de trabajo - 2026-08-18

## Acuerdo de documentación

- Se documentará en este archivo todo lo que se haga durante el día para mantener contexto del trabajo.
- Cada día se creará un archivo Markdown nuevo dentro de `docs/` con el formato `YYYY-MM-DD.md`.
- Cada cambio relevante se añadirá conforme se vaya realizando.

## Cambios

- Creado este registro diario inicial en `docs/2026-08-18.md`.
- Revisado el estado técnico inicial del proyecto.
- Comprobado que el proyecto usa React 19, TypeScript, Vite, PWA con `vite-plugin-pwa`, Capacitor y Supabase.
- Comprobado estado de Git: rama `main` alineada con `origin/main`; solo aparece sin seguimiento la carpeta `docs/` creada para documentación.
- Revisados scripts disponibles: `dev`, `build`, `mobile:build`, `cap:sync`, `cap:open:ios`, `cap:open:android`, `lint`, `preview` y `logos:generate`.
- Ejecutado `npm run build`: correcto. TypeScript y build de Vite pasan.
- Ejecutado `npm run lint`: falla con 5 errores y 5 warnings.

## Hallazgos técnicos iniciales

- `src/hooks/useGroups.ts`: `checkPendingInviteModal` recibe `_userId` pero no lo usa. ESLint lo marca como error `@typescript-eslint/no-unused-vars`.
- `src/hooks/useSettlements.ts`: React Hooks/Compiler marca `goToMonth` por no poder preservar memoización manual con `useCallback`.
- `src/utils/notifications.ts`: hay tres bloques `catch` vacíos que ESLint marca como error `no-empty`.
- `src/App.tsx`: warning de dependencia faltante en un `useEffect` relacionado con `subs`.
- `src/hooks/useSubscriptions.ts`: varios warnings de dependencias faltantes en `useCallback`, principalmente `setSubscriptions`, `groupCustomShares` y `groupSplitMode`.
- `capacitor.config.ts`: contiene `server.url` local hardcodeado (`http://172.20.10.11:5174`) con `cleartext: true`; conviene revisarlo antes de builds móviles para App Store/Play Store.
- `npm run build` muestra aviso de Browserslist/caniuse-lite desactualizado.

## Estado de validación inicial

- Build producción: pasa.
- Lint: falla inicialmente.
- No se han aplicado correcciones de código todavía; solo diagnóstico y documentación.

## Corrección de lint

- Corregido `src/hooks/useGroups.ts`: eliminado el parámetro no usado de `checkPendingInviteModal`.
- Corregido `src/App.tsx`: actualizada la llamada a `checkPendingInviteModal` y extraídos `subscriptionCount`/`openSubscriptionForm` para satisfacer dependencias de hooks.
- Corregido `src/hooks/useSettlements.ts`: añadidas dependencias explícitas al `useCallback` de navegación mensual.
- Corregido `src/hooks/useSubscriptions.ts`: añadidas dependencias faltantes en callbacks (`setSubscriptions`, `groupCustomShares`, `groupSplitMode`).
- Corregido `src/utils/notifications.ts`: añadidos comentarios en bloques `catch` intencionadamente silenciosos para cumplir `no-empty`.

## Validación tras correcciones

- Ejecutado `npm run lint`: correcto, sin errores ni warnings.
- Ejecutado `npm run build`: correcto.
- En ese momento, el build mantenía el aviso externo de Browserslist/caniuse-lite desactualizado.

## Pendientes actuales

- Sin pendientes técnicos abiertos de la revisión inicial.

## Revisión de Capacitor y Browserslist

- Revisado `capacitor.config.ts`: eliminado el bloque `server` con `url` local (`http://172.20.10.11:5174`) y `cleartext: true` para evitar que builds móviles apunten a una máquina local.
- Ejecutado `npx cap sync`: Android e iOS quedaron sincronizados sin la URL local en sus `capacitor.config.json` generados.
- `npx cap sync` actualizó archivos Gradle generados para incluir `@capacitor/local-notifications`, coherente con el plugin usado por la app.
- Ejecutado `npx update-browserslist-db@latest`: actualizado `caniuse-lite` y `baseline-browser-mapping` en `package-lock.json`.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run mobile:build`: correcto; incluye `npm run build` y `npx cap sync`.
- El aviso de Browserslist/caniuse-lite desactualizado ya no aparece en el build.

## Pasada funcional básica

- Levantado servidor de desarrollo con `npm run dev -- --host 127.0.0.1 --port 5173`.
- Comprobado por HTTP que `/` responde `200 OK` y sirve el HTML principal de Notifyra.
- Comprobado por HTTP que `/src/main.tsx`, `/src/App.tsx`, `/src/App.css` e `/icon.svg` responden correctamente desde Vite.
- Revisado log de Vite: arranque correcto sin errores reportados.
- Revisados flujos principales desde código: arranque con `ErrorBoundary`, autenticación/local mode, dashboard, listado de suscripciones, formulario, calendario, ajustes, grupos y persistencia en `localStorage`.
- Detectado detalle de PWA: la manifest generada tenía `lang: "en"` por defecto aunque la app está en español.
- Corregido `vite.config.ts`: añadida propiedad `lang: 'es'` en la manifest PWA.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run mobile:build`: correcto; regeneró `dist/`, Android e iOS con `manifest.webmanifest` usando `lang: "es"`.
- Servidor de desarrollo detenido al finalizar la comprobación.
- Limitación: no se pudo hacer una prueba visual/clic a clic en navegador automatizado desde las herramientas disponibles; la pasada fue por servidor HTTP, build y revisión de flujos en código.

## Servidor local para revisión manual

- El servidor de desarrollo se había detenido al finalizar la pasada funcional, por eso `http://127.0.0.1:5173/` devolvía `ERR_CONNECTION_REFUSED`.
- Comprobado que el puerto `5173` estaba libre.
- Levantado de nuevo el proyecto con `npm run dev -- --host 127.0.0.1 --port 5173`.
- Verificado por HTTP que `http://127.0.0.1:5173/` responde `200 OK`.
- El servidor queda levantado para que el usuario pueda revisar la app manualmente en el navegador.

## Acceso de prueba en desarrollo

- Añadido botón `Probar sin login` en la pantalla de autenticación.
- El botón solo se renderiza cuando `import.meta.env.DEV` es `true`, es decir, en desarrollo con Vite.
- En producción (`npm run build`) el texto y la lógica del botón no aparecen en los archivos JS/HTML generados.
- El acceso de prueba crea una sesión local de desarrollo sin usuario de Supabase y usa el email `dev@notifyra.local` si no había email introducido.
- Al cerrar sesión se limpia también la marca interna de sesión de desarrollo.
- Añadido estilo específico `.auth-dev-login` para diferenciar el botón de prueba.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Comprobado que `http://127.0.0.1:5173/` sigue respondiendo `200 OK` para probarlo manualmente.

## Refinamiento visual móvil

- Revisada la captura de la pantalla de ajustes en navegador móvil.
- El menú abierto de un elemento HTML `select` es un control nativo del navegador/sistema y sus opciones flotantes no se pueden estilizar de forma consistente con CSS estándar.
- Personalizado el campo cerrado de los `select`: se eliminó la apariencia nativa, se añadió una flecha propia y se ajustaron altura, padding y radio.
- Reducidos los tokens globales de radio para evitar una estética excesivamente redondeada en tarjetas, campos y botones.
- Ajustados el espaciado y los controles de la pantalla de ajustes para una composición más compacta.
- Refinada la tarjeta de cuenta y cambiado su avatar circular por una forma rectangular compacta.
- Rediseñado el botón `+` de la navegación inferior como una pieza rectangular compacta con radio menor y degradado.
- Rediseñado el botón de añadir en la lista de suscripciones con la misma estética.
- Sustituido el carácter `+` del listado por el icono `Plus` de Lucide para mantener consistencia visual.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Verificado que el servidor local continúa activo y responde `200 OK` en `http://127.0.0.1:5173/`.

## Segunda revisión visual

- El primer cambio de radios se consideró demasiado sutil y el fondo rectangular de los botones `+` no era el resultado deseado.
- Reducidos de nuevo los radios globales a valores claramente más rectos: `2px`, `4px`, `6px` y `8px` según jerarquía.
- Eliminado completamente el fondo, degradado, sombra y contenedor visible del botón `+` de la navegación inferior.
- Eliminado completamente el fondo, degradado, sombra y contenedor visible del botón `+` de la lista de suscripciones.
- Aumentado ligeramente el tamaño de ambos iconos `Plus` y afinado su trazo para que funcionen como iconos independientes.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Servidor local verificado en `http://127.0.0.1:5173/` con respuesta `200 OK`.

## Desplegables personalizados

- Creado `src/components/CustomSelect.tsx` como componente reutilizable para evitar los menús nativos inconsistentes entre navegadores.
- El componente permite abrir/cerrar al tocar, seleccionar una opción y cerrar al tocar fuera.
- Añadido soporte de teclado con flechas, Inicio, Fin, Enter, Espacio y Escape.
- Añadidos atributos ARIA de `listbox`, opción activa y opción seleccionada.
- Sustituidos en Ajustes los `select` nativos de Tema, Moneda y Recordatorios.
- Añadidos estilos propios para el disparador, flecha, menú flotante, opción activa y marca de selección.
- Verificado que `SettingsView.tsx` ya no contiene elementos `select` ni `option` nativos.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Servidor local activo y verificado en `http://127.0.0.1:5173/`.

## Punto estable y pruebas automatizadas

- Creado commit `adfa491` con mensaje `feat: improve local testing and mobile experience`.
- La rama `main` quedó un commit por delante de `origin/main`; no se realizó push.
- Añadido Vitest como dependencia de desarrollo.
- Añadido script `npm test` para ejecutar la batería en modo no interactivo.
- Creado `src/utils/date.test.ts` con pruebas de formato ISO, diferencias de días, límites de mes, años bisiestos y ciclos de recurrencia.
- Creado `src/utils/subscription.test.ts` con pruebas de recordatorios, reparto exacto en céntimos, claves de pago, selección de resultados y totales por periodo.
- Ejecutado `npm test`: 2 archivos y 13 pruebas correctas.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- La instalación de Vitest reportó 21 vulnerabilidades en el árbol de dependencias; queda pendiente analizarlas antes de aplicar actualizaciones automáticas.

## Auditoría de dependencias

- Ejecutados `npm audit`, `npm audit --omit=dev` y `npm outdated` para separar riesgos de producción y desarrollo.
- Estado inicial: 21 vulnerabilidades totales, de las cuales 5 afectaban al árbol instalado en producción.
- Aplicadas únicamente correcciones compatibles mediante `npm audit fix`, sin usar `--force` ni introducir versiones mayores directas.
- La primera pasada redujo las alertas de 21 a 1; la restante pertenecía al árbol de ESLint.
- La segunda pasada actualizó `brace-expansion` y eliminó la última alerta.
- Estado final de `npm audit`: cero vulnerabilidades.
- Estado final de `npm audit --omit=dev`: cero vulnerabilidades.
- Vite quedó actualizado dentro del rango compatible de `7.3.1` a `7.3.6`.
- Ejecutado `npm test`: 13 pruebas correctas.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Reiniciado el servidor local del proyecto para cargar Vite `7.3.6`; responde `200 OK` en `http://127.0.0.1:5173/`.

## Pruebas E2E móviles

- Añadido Playwright y descargado Chromium localmente dentro de `node_modules`.
- Creado `playwright.config.ts` con emulación de Pixel 7 y servidor aislado en `127.0.0.1:4174`.
- Añadido script `npm run test:e2e`.
- Añadidas salidas `playwright-report` y `test-results` a `.gitignore`.
- Creado `e2e/app.spec.ts` con prueba de acceso local, navegación y cambios en los desplegables personalizados de Ajustes.
- Añadida prueba E2E del ciclo completo local: crear, editar y eliminar una suscripción.
- Interceptadas las llamadas a iTunes durante E2E para evitar dependencia de red externa y resultados inestables.
- Separadas las suites: `npm test` ejecuta únicamente Vitest dentro de `src`; `npm run test:e2e` ejecuta Playwright.
- Detectada una carrera al ejecutar ESLint y Playwright en paralelo sobre la carpeta temporal `test-results`.
- Añadidos `playwright-report` y `test-results` a los ignores globales de ESLint.
- Ejecutado `npm test`: 13 pruebas unitarias correctas.
- Ejecutado `npm run test:e2e`: 2 pruebas E2E correctas.
- Ejecutado `npm run lint`: correcto, también en paralelo con Playwright.
- Ejecutado `npm run build`: correcto.
- Ejecutado `npm audit`: cero vulnerabilidades.

## Auditoría de Supabase

- Revisados `supabase/schema.sql`, las migraciones existentes y las 35 llamadas a tablas/RPC desde el frontend.
- Detectado que el constraint de `reminder_days` solo permitía `1`, `3` y `7`, mientras la UI permite valores entre `0` y `14` y la lógica admite hasta `30`.
- Detectado que la UI permite frecuencia trimestral para gastos de grupo, pero el constraint de `group_expenses` la rechazaba.
- Detectado que los gastos de grupo anulados se recargaban porque faltaba filtrar `anulado = 0`.
- Detectado que la política de actualización de invitaciones permitía al invitado modificar más columnas que el estado.
- Detectado que una membresía antigua podía reactivarse conservando un rol administrativo anterior.
- Detectado que la política de salida de miembros permitía actualizar directamente todos los campos de la membresía propia.
- Detectado que pagadores y participantes podían referenciar miembros de otro grupo sin una validación cruzada adicional.
- Detectado que cualquier miembro podía cerrar una liquidación aunque políticas y comentarios indicaban administradores/propietarios.
- Detectado que `settled_by` podía impedir eliminar una cuenta por la clave foránea sin `on delete set null`.
- Detectado que varias funciones `security definer` conservaban el permiso `EXECUTE` por defecto de PostgreSQL para `PUBLIC`.
- Detectado que una instalación basada únicamente en `schema.sql` no incluía columnas, RPC y tablas añadidas posteriormente.

## Endurecimiento de Supabase

- Creada la migración autocontenida `supabase/migrations/20260818_harden_permissions_and_constraints.sql`.
- Ampliado `reminder_days` al rango `0..30` y añadida frecuencia `trimestral` para grupos.
- La migración añade, si faltan, logos, hora de aviso, anulado, liquidaciones, triggers, índice y política de lectura de perfiles compartidos.
- Añadidas funciones para comprobar que pagadores y participantes pertenecen al grupo real del gasto/cobro.
- Reemplazadas políticas de inserción/actualización para validar esas relaciones.
- Eliminada la actualización directa amplia de invitaciones por parte del invitado.
- Añadido RPC restringido `decline_group_invite_by_id` y actualizado el frontend para usarlo.
- Añadido RPC `get_group_invite_preview` para previsualizar enlaces genéricos sin exponer directamente la tabla.
- Los RPC de aceptación validan el email salvo en enlaces genéricos y reactivan siempre como `member`, excepto al propietario real.
- Eliminada la política insegura de actualización libre de membresía propia.
- Las liquidaciones solo pueden cerrarlas administradores/propietarios y solo mediante RPC.
- Añadida validación del periodo y control de concurrencia para liquidaciones duplicadas.
- Cambiada la referencia `settled_by` a nullable con `on delete set null`.
- Revocados permisos `PUBLIC`/`anon` de funciones sensibles y concedidos explícitamente a `authenticated`.
- El frontend oculta la acción de liquidar a miembros normales y muestra `Ver detalle` en su lugar.
- Añadidos mensajes específicos para falta de autorización y periodos inválidos.
- Corregido el seed de miembros ficticios para que no falle si esos usuarios no existen en el entorno.
- Actualizado README con el orden de instalación para proyectos nuevos y existentes.
- Ejecutado `npm test`: 13 pruebas correctas.
- Ejecutado `npm run test:e2e`: 2 pruebas correctas.
- Ejecutado `npm run lint`: correcto.
- Ejecutado `npm run build`: correcto.
- Ejecutado `npm audit`: cero vulnerabilidades.
- No se ejecutó la migración contra Supabase: no hay cliente PostgreSQL/base local ni conexión administrativa disponible. Debe aplicarse antes de desplegar el frontend que usa los RPC nuevos.

## Revisión de ruta de trabajo

- Se comprobó que `/Users/MAMP/htdocs/gestionsub` y `/Users/franverdugo/MAMP/htdocs/gestionsub` no existen.
- Se encontró otra copia del proyecto en `/Applications/MAMP/htdocs/gestionsub`.
- La copia de MAMP apunta al mismo remoto `https://github.com/FranR17/gestionsub.git`.
- La copia de MAMP está en `2257de4` y alineada con `origin/main`.
- La copia actual de trabajo está en `/Users/franverdugo/Sites/localhost/gestionsub`.
- Tras ejecutar `git fetch origin`, la copia actual quedó divergida: 1 commit por delante y 5 commits por detrás de `origin/main`.
- El commit propio de la copia actual es `adfa491 feat: improve local testing and mobile experience`.
- La copia de MAMP tiene cambios locales sin commit en código, Android, README, Supabase y nuevos archivos de administración/incidencias.
- Se detuvo cualquier mezcla automática para evitar sobrescribir trabajo existente en cualquiera de las dos copias.
